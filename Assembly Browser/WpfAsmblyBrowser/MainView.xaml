<Window x:Class="WpfAsmblyBrowser.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAsmblyBrowser"
        mc:Ignorable="d"
        Title="Assembly Browser" Height="450" Width="800">
    <Window.DataContext>
        <local:MainPageVM/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" Height="20" VerticalAlignment="Center" Foreground="Black" Background="White">
            <MenuItem Header="File">
                <MenuItem Header="Open"/>
            </MenuItem>
        </Menu>
        <TreeView Grid.Row="1" Background="AntiqueWhite" Margin="0,0,0,10">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate>
                    <TextBlock Text="asdad"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="File:"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
